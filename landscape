var landscape = function(){
var result ="";
var flat = function (size) {
for ( var count = 0; count < size; count ++)
result += "/";
for ( var count = 0; count < size; count++)
result +="";
result += "\\";};
flat(3);
mountian(4);
flat(6);
mountian(1);
flat(1);
return result;};
